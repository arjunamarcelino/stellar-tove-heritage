import { describe, it, expect } from 'vitest';

describe.skip('Artworks (integration) — enable when TOV-189 schema lands', () => {
  it('lists verified artworks from DB', () => {
    expect(true).toBe(true);
  });
});
