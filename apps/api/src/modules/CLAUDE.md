# Modules

Feature modules following Clean Architecture. Each module is self-contained with its own controller, service, DTOs, entities, and repository.

## Module Structure

```
modules/{name}/
  {name}.module.ts           # NestJS module definition
  {name}.controller.ts       # HTTP routes
  {name}.service.ts          # Business logic
  dto/
    create-{name}.dto.ts     # Input DTO
    update-{name}.dto.ts     # Partial input DTO
    {name}-response.dto.ts   # Output DTO (never expose entities directly)
  entities/
    {name}.entity.ts         # TypeORM entity extending BaseEntity
  repositories/
    {name}.repository.ts             # TypeORM implementation
    {name}-repository.interface.ts   # Interface contract
```

## Adding a New Module

1. Create the directory structure above
2. Entity extends `BaseEntity` from `src/common/entities/`
3. Repository extends `BaseRepository<T>` and implements its own interface
4. Service injects the repository via interface token
5. Controller uses `@Public()` decorator as needed; backoffice controllers use `@UseGuards(BackofficeGuard)` + `@AdminRoles()`. `@Controller()` takes only the resource name (e.g. `'admins'`) -- the `api/v1` / `api/backoffice/v1` prefix comes from `RouterModule`, not the decorator
6. Module uses `TypeOrmModule.forFeature([Entity])` -- do not re-import `DatabaseModule`
7. Add the leaf module to the right surface's list -- `PUBLIC_MODULES` in `public-api.module.ts` (`api/v1`) or `BACKOFFICE_MODULES` in `backoffice/backoffice.module.ts` (`api/backoffice/v1`). That single array feeds both `RouterModule.register({ children })` and the Swagger `include`. Provider-only modules (no controller) can stay imported directly in `app.module.ts`
8. Add error codes to `ErrorCode` enum (prefixed with domain name)
9. Create migration for the new table (with partial indexes for soft deletes)

## Sanctioned patterns (don't "fix" these back)

- **Raw-body PATCH validation (TOV-30, `users/profile`):** `PATCH /me` reads the RAW `req.body` via `@Req()` (NOT `@Body() dto`) and validates in the service (`validateAndBuildPatch` → `failValidation`). Reason: reliable null-vs-absent presence detection under SWC `useDefineForClassFields`, plus the AC-mandated 422 + `errors[]` shape the global 400 `ValidationPipe` can't emit. The `UpdateMeProfileDto` exists for Swagger (`@ApiBody`) only. Prefer `@Body() dto` + class-validator everywhere else; this is the documented exception.

## API Surfaces

Controllers are served under two `RouterModule` prefix trees, grouped by module:

- **Public** (`api/v1/...`) -- leaf modules in `PUBLIC_MODULES` (`public-api.module.ts`)
- **Backoffice** (`api/backoffice/v1/...`) -- leaf modules in `BACKOFFICE_MODULES` (`backoffice/backoffice.module.ts`)

A domain shared by both surfaces (e.g. **files/**) lives in its own neutral module that exports its
service; each surface imports it and declares its own controller on top. Neither surface imports the
other. See root `CLAUDE.md` -> "API Surfaces" for the full rationale.

## Current Modules

- **auth/** -- public JWT authentication (login, register, refresh, logout, profile) [`api/v1/auth`] plus **SEP-10 wallet auth** (BYOW): `auth/sep10/challenge` + `auth/sep10/verify`, offline Stellar signature verification, mints wallet-only (null-email) users. Also **embedded passkey registration** (TOV-21): `auth/passkey/register/begin` + `.../finish`, WebAuthn enroll + Soroban smart-wallet deploy. See `auth/CLAUDE.md`.
- **wallets/** -- neutral wallet domain (`Wallet` entity + repo + `WalletsService.findOrCreateForWallet`, atomic user+wallet bind; `Wallet.kind` includes `embedded_passkey`). Owns the `PasskeyCredential` entity + repo (1:1 `Wallet`), `createEmbeddedPasskeyWallet` (atomic user + wallet + credential bind), `resolveEmbeddedWalletForUser` (owner-scoped transfer resolution), and `cose.helper.ts` (COSE->raw-P256). Provider-only + neutral; consumed by `auth` (SEP-10 + passkey). The **`transfer/` subfolder** (TOV-22) is a public surface on top: `PublicWalletTransferModule` -> `WalletTransferController` (`POST api/v1/wallet/transfer/build` + `/submit`, authenticated + owner-scoped) + `WalletTransferService`, added to `PUBLIC_MODULES` (the `files/` precedent). The **`me/` subfolder** (TOV-24) is the authenticated identity surface: `PublicMeWalletsModule` -> `MeWalletsController` on `@Controller('me/wallets')` — `GET /me/wallets` (list), `POST /me/wallets/challenge` (user-bound SEP-10 challenge), `POST /me/wallets` (idempotent BYOW add, `Idempotency-Key` required; **TOV-32** enriches the 201 with an optional `trustlineRequired: { changeTrustXdr, asset }` when the bound classic G-account lacks the USDC trustline — an unsigned **seq=0** `change_trust` template the FE signs), `POST /me/wallets/:id/primary` (TOV-25 set primary settlement wallet — active-byow-only, idempotent no-op on re-set, writes a `PRIMARY_CHANGED` audit row), `DELETE /me/wallets/:id` (soft-unbind → `200 {deletedId, newPrimaryWalletId}`; when the target is primary, auto-promotes the oldest eligible byow sibling and reports it, else 409 `PRIMARY_WALLET_CANNOT_BE_REMOVED`). Set-primary + auto-promote run through the shared **`WalletsService.runWithPrimaryContention`** helper — the ONE lock-free primary-concurrency strategy (optimistic `UQ_wallets_primary_active` 23505-catch + demote-retry, no `FOR UPDATE`), also used by bind/reactivate. Orchestration in `MeWalletsService`; the module imports `AuthModule` (for `Sep10Service`, the acyclic direction) + `IdempotencyModule` (`@common/idempotency`, backed by `@config/redis.config`) + the export module + the neutral audit module + the read-only **`WALLET_TRUSTLINE_SERVICE`** port (`SorobanWalletTrustlineService`, at the neutral `wallets/` root, bound in `PublicMeWalletsModule` — mirrors `FRACTION_READ_SERVICE`; `FakeWalletTrustlineService` for tests) for the TOV-32 trustline check. The trustline resolve is a **TOTAL/fail-open** enrichment run in `buildAddResponse` AFTER `idempotency.complete()` and OUTSIDE the `fail()`-guarded try (a throw there would delete the completed record → re-run the single-use challenge); reads the classic trustline via Soroban `getLedgerEntries` (no Horizon) and is re-resolved fresh on both fresh + replay paths (never cached → non-byte-identical replays). See `docs/solutions/integration-issues/soroban-classic-trustline-read-and-seq0-changetrust-template.md`. `MeWalletsController` ALSO carries the export routes (`:id/export...`) so `DELETE :id` and `GET :id/export` share one controller's static-before-`:param` route ordering. The **`export/` subfolder** (TOV-40) is now **provider-only**: `WalletExportModule` provides + exports `WalletExportService` (the N-transfer drain) — no controller. Export is **non-atomic N single-token transfers** (Soroban = 1 op/tx; the wallet contract has only `transfer()`), so it is stateful/resumable: `wallet_exports` + `wallet_export_items` trackers, single-writer item CAS claim, `FOR UPDATE` completion latch + live-balance-zero gate, and status-read reconciliation for the crash window. Owns `fraction_kyc_allowlist` (read-only KYC gate). Reason→HTTP mapping shared via `@common/http/fail-http` (relayer/`transfer-error-http.ts` re-exports it). See `docs/solutions/integration-issues/soroban-embedded-wallet-export-n-transfer-orchestration.md`. The **`audit/` subfolder** (extracted TOV-25 #158) is the neutral append-only audit facility: `WalletsAuditModule` owns the `internal_audit_log` entity + repo + `AuditLogService` (DB-trigger-enforced append-only), imported by `wallets`, `export`, and `me`. `AuditLogService.record(entry, manager?)` writes in the caller's transaction when a manager is passed (atomic with the audited side effect). `WalletsService` emits genesis `PRIMARY_CHANGED` (`reason:'initial'`) rows on first primary designation (login / passkey / add self-heal / reactivation); the me-surface emits `reason:'user'` (set-primary) and `reason:'auto_promote'` (delete) rows. The **`rotation/` subfolder** (TOV-33, FR-01.12) is the provider-only wallet-**rotation** holdings-transfer stack — **copy-adapted** from `export/` (deliberately NOT a shared engine): `WalletRotationModule` provides + exports `WalletRotationService`, and `MeWalletsController` ALSO carries its routes (`:id/rotate-transfer` [initiate], `/submit`, `GET /status`, `DELETE` [cancel]) — moving ALL non-zero FractionToken balances from an **embedded passkey source** to the caller's **owned BYOW primary** as N single-op transfers with the same resumable spine (per-item CAS, `FOR UPDATE` completion latch + live-balance-zero gate over the frozen fraction set, status-read reconcile that also promotes false-negative `failed`-but-drained items). Forks vs export: enumerate the deployed `fraction_contracts` registry via the **bounded `FRACTION_READ_SERVICE.balancesOf`** (reads the SOURCE contract explicitly, NEVER `resolvePrimarySettlementAddress` which points at the empty destination); destination pre-flight is the **authoritative on-chain `KYC_ALLOWLIST_TX_SERVICE.isAllowed`** (throws on RPC → recoverable per-item fail, never a wedge — PR #56 #429); a **write-safe lockup gate** reads the persisted deploy-time `fraction_contracts.artist_lockup_until` (fires only when the source C-address == the contract's `artist_address`; on-chain `check_lockup_floor` is the hard backstop — #433); one append-only **`registry_events` `custody_transfer`** row per confirmed transfer written ATOMICALLY with the item-confirm (FULL-unique `source_ref = rotation_item:{id}` + `(rotation_id, token_contract)` unique so keyless concurrent initiate can't double-write provenance — #428); the source is NEVER latched `exported` (rotation only moves — removal is the separate `DELETE :id`). **Bidirectional** cross-feature mutual-exclusion (an active export blocks a rotation and vice-versa, `In(['pending','submitting'])`) is done **table-level on both sides** (each module `forFeature`-registers the other's tracker entity) to stay module-cycle-free (#431). `claimItemForSubmit` filters `deleted_at IS NULL` so a concurrent `cancel` (soft-delete) can't be raced into moving money (#436). `WalletRotationModule` binds `FRACTION_READ_SERVICE` + `FRACTION_CONTRACT_REPOSITORY` directly (dependency-surface minimization; not `FractionalizationModule`) + imports `KycAllowlistModule`. New `ROTATION_*` error codes + `wallet.rotation.*` audit kinds; migrations `1716000000052` (fraction_contracts.artist_lockup_until + backfill, persisted by the deploy worker) / `053` (rotation trackers) / `054` (append-only registry_events) / `055` (item token-unique) / `056` (status-poll index). Shared `ledger-number.transformer.ts` at the `wallets/` root (export item + rotation item + registry). **End-to-end blocked on TOV-243** (a BYOW G-address must be added to the on-chain KYCAllowlist first — `add`/`is_allowed` accept any Address; TOV-235's admin encoder is C-address-only). See `docs/solutions/integration-issues/copy-adapting-a-resumable-passkey-drain-money-path-pitfalls.md` + PR #56 review todos `428-437`.
- **relayer/** -- provider-only module behind the `RELAYER_SERVICE` port (`SorobanRelayerService`). (1) **Deploy** (TOV-21): deploys passkey-bound Soroban smart-wallets by invoking the real testnet `FractionWalletFactory.deploy_wallet` (single `Signer::External` passkey signer), serialized per account (txBAD_SEQ/throttle transparently retried, RPC calls timeout-bounded) and idempotent via `salt = sha256(rawCredentialId)` + off-chain derivation with an on-chain `walletExists` re-check self-heal, guarded by a Redis `RELAYER_ACCOUNT_LOCK` (one shared key per relayer keypair for deploy AND transfer; in-memory fallback for tests). (2) **Passkey-signed USDC transfer** (TOV-22): `buildTransfer` (simulate-only, returns the tx + the OZ `auth_digest` WebAuthn challenge) + `submitSignedTransfer` (fail-closed `verifyPasskeyAuthorization` -> attach the OZ `AuthPayload` -> re-simulate -> fee cap -> send-only lock -> poll). Pure helpers: `secp256r1.ts` (low-S), `auth-entry-encoding.ts` (OZ `stellar-accounts` v0.7.2 `AuthPayload`, golden-vector-pinned), `passkey-authorization.ts` (now takes optional `expectedTo`/`expectedAmountScaled` — the export surface pins the recipient + exact amount server-side; the transfer surface omits them). (3) **Export reads** (TOV-40): `readWalletHoldings` (read-only `balance()` simulate per token, fail-closed). Token display metadata is config-driven (`RELAYER_FRACTION_TOKENS`), NOT read from chain, to keep display-only concerns off the fail-closed money port. Consumed by `auth` (deploy) + `wallets/transfer` (transfer) + `wallets/export` (holdings read + per-item transfer); tests override with `FakeRelayerService`. See `auth/CLAUDE.md`.
- **users/** -- user domain (no controller; admin HTTP in `backoffice/users`). Email/password are nullable (BYOW wallet-only users). The **`handle/` subfolder** (TOV-26, FR-01.05) is a public surface layered on top: `PublicHandleModule` (in `PUBLIC_MODULES`) with `MeHandleController` (`@Controller('me/handle')`, authenticated GET+POST — read/set the caller's handle, freely-changeable upsert) and `HandlesController` (`@Controller('handles')`, `@Public()` `GET /handles/check` — IP-throttled availability). Handle is a column on `users` (`handle` + DB-generated `handle_canonical = lower(handle)` with partial unique index `WHERE handle_canonical IS NOT NULL AND deleted_at IS NULL`); `UserRepository` gains `findByHandleCanonical`/`setHandle`, `UsersModule` exports the `USER_REPOSITORY` token. Case-insensitive uniqueness + concurrency enforced by the index (23505 → 409 `HANDLE_TAKEN`); format/reserved validated in `HandleService` (→ 422 `HANDLE_FORMAT_INVALID`/`HANDLE_RESERVED`, since the global ValidationPipe emits 400). Pure rules in `handle-format.ts`.
- **collectors/** -- public (`@Public()`) pseudonymous collector profile (TOV-27, FR-01.06) in `PUBLIC_MODULES`: `CollectorsController` (`@Controller('collectors')`, `GET /collectors/:handle`, IP-throttled 30/min) + `CollectorsService`. Resolves the CURRENT handle only, case-insensitively via the **projected** `UserRepository.findPublicProfileByHandleCanonical` (no secret columns hydrated on a public path); old/unknown/soft-deleted/over-length all return an identical `404 COLLECTOR_NOT_FOUND` (no existence oracle). Returns `{ handle, previousHandles[], createdAt }` where `createdAt` is a member-since **date** (`YYYY-MM-DD`, date granularity to limit fingerprinting) and `previousHandles` is deduped-by-canonical, newest-first, current excluded, and `[]` when the collector set `handle_history_public=false`. Imports the neutral `UsersModule` for `USER_REPOSITORY` + `HANDLE_HISTORY_REPOSITORY`. Handle history append + immutability live under `users/` (see `users/CLAUDE.md`).
- **kyc/** -- KYC submission + envelope-encrypted storage (TOV-28, FR-01.07) in `PUBLIC_MODULES`: `PublicKycModule` -> `KycController` (`@Controller('me/kyc')`, authenticated + owner-scoped) with `POST /me/kyc/submissions` (multipart 4 docs + `claimed_jurisdiction`, `Idempotency-Key` required, throttled 5/hr → `202 pending_review`), `GET /me/kyc` (status + latest submission metadata; never bytes/keys/URLs), and `GET /me/kyc/status` (TOV-29, FR-01.08 whitelist status card; throttled 30/60s → `{ status, whitelistedAt, reason, lastSubmissionAt }`, all keys always present with `null` when N/A). **Whitelist lifecycle (TOV-29):** the user-level `KycStatus` enum lives in `@common/enums/kyc-status.enum` (so the neutral `users` entity imports it *down*, not up into this feature module) and now holds the 5-state whitelist vocabulary (`not_submitted`/`pending_review`/`whitelisted`/`frozen`/`removed`) — distinct from the unchanged per-submission `KycSubmissionStatus` (`pending_review`/`approved`/`rejected`). `WhitelistStatusResponseDto.build` gates fields via a compile-exhaustive `satisfies Record<KycStatus,…>` map: `whitelistedAt` only when `whitelisted`, `reason` (a machine-readable CODE, never raw admin prose — M12 writes it) only when `frozen`/`removed`; a stale `whitelisted_at` on a frozen row is never surfaced. New nullable `users.whitelisted_at` + `users.kyc_reason` columns (migration `1716000000026`, backfill-before-VALIDATE, guarded `down()`); real transitions are M12/Phase 2. **Envelope encryption** in `KycCryptoService`: per-document `randomBytes(32)` DEK; AES-256-GCM blob with `AAD = userId|submissionId|docType`; DEK wrapped by the `KYC_MASTER_KEY` KEK via `IKeyWrapper`/`ConfigKeyWrapper` (context-bound GCM, AAD binds `…|keyVersion` for rotation). `blob_hash` is a **keyed HMAC** and `submissionId` a **secret-keyed HMAC** (both HKDF sub-keys of the master key, so neither the DB value nor the object key is a reconstructable/offline oracle). Crypto is **cooperatively chunked** (64KB + `setImmediate`) so ~40MB submits don't block the event loop; `KycConcurrencyInterceptor` (listed BEFORE Multer) caps concurrent submits (`KYC_MAX_CONCURRENT_SUBMISSIONS`, 503 backpressure); `MulterExceptionFilter` maps interceptor-level Multer errors to `KYC_*` codes. **Storage:** shared `KycStorageModule` binds the private `tove-kyc` bucket to the neutral `SupabaseStorageService` via the `STORAGE_BUCKET_OVERRIDE` DI token (`KYC_STORAGE` = `IKycStorageService`; no cloned service). **Persistence:** `kyc_submissions` + `kyc_documents` (soft-delete; FK `ON DELETE RESTRICT` for 7-yr retention; partial-unique `UQ_kyc_submissions_one_pending_per_user` is the race backstop) + a `kyc_status` column on `users`; the terminal-state flip + inserts + audit row run in one txn (idempotency `begin` precedes the resubmit-policy check so a legit replay returns the original 202). Reuses the shared `IdempotencyStore` (`@common/idempotency`) and the neutral `WalletsAuditModule` (audit `kind='kyc.submission.created'`, written inside the submit txn). The **`sweep/` subfolder** (provider-only `KycSweepModule`, imported by `app.module`) is the orphan-blob reaper: a `kyc-orphan-sweep` BullMQ repeatable job (`KycSweepScheduler` + `KycOrphanSweepProcessor`) that lists bucket objects older than `KYC_ORPHAN_GRACE_HOURS` and deletes those with no `kyc_documents.storage_key` (incl. soft-deleted rows = retained). Config in `kyc.config.ts`. See `docs/solutions/code-review-resolutions/pr30-tov28-kyc-submission-review-resolutions.md`.
- **fractionalization/** -- per-artwork FractionToken deploy domain (TOV-233, FR-04.MVP.01). Provider-only + neutral **except** the public authenticated `me/holdings` read surface (see the `me/` subfolder below). Owns the persisted `Artwork` (internal `status`: `verified|published|fractionalizing|fractionalized`; the public browse in `artworks/` now reads this table directly via its own `ARTWORK_READ_REPOSITORY` — TOV-189) + the net-new `ArtworkImage` entity (supporting images for the public detail, TOV-189, migration `1716000000046`; co-located here as the same aggregate, consumed read-only by `artworks/`) + `FractionContract` entities/repos, and the **`FRACTION_FACTORY_SERVICE`** port (`SorobanFractionFactoryService`; `FakeFractionFactoryService` for tests). The service deploys via the on-chain `FractionTokenFactory` (TOV-137): admin-gated `deploy(TokenInit)` signed by a **separate** `FRACTION_FACTORY_ADMIN_SECRET` (`authorizeEntry`) with a distinct `FRACTION_RELAYER_SECRET` as tx source on its own `relayer:fraction:account` lock key; `token_of` registry read is the self-heal oracle; a config-gated `onApplicationBootstrap` probe asserts `factory.admin()==admin` + relayer funded. The **`deploy/` subfolder** is the provider-only worker (`FractionDeployWorkerModule`, imported by `app.module`): `FractionDeployProcessor` (`concurrency:1`, poll outside the lock) + a promote-only `FractionReconcileProcessor` sweeper + scheduler. The HTTP surface is `backoffice/artworks/` (`POST :id/fractionalize` → 202 + enqueue, `GET :id/fractionalization`; plus **admin read** TOV-240: `GET /artworks` paginated list [default filter `verified|fractionalizing|fractionalized`, CSV `status` narrows] + `GET /artworks/:id` detail, both projecting the **active-only** `fraction_contracts` row [`findActiveByArtworkId(s)`, excludes `failed` so a prior failed deploy → `fractionContract:null` re-shows the CTA]; camelCase DTOs via `@common` `@ApiPaginatedResponse`), in `BACKOFFICE_MODULES`. The **`me/` subfolder** (TOV-237, FR-04.MVP.04) is the public authenticated read surface layered on this neutral domain: `PublicMeHoldingsModule` -> `MeHoldingsController` on `@Controller('me/holdings')` (in `PUBLIC_MODULES`), `GET /me/holdings` -> the caller's fraction holdings. `MeHoldingsService` resolves the primary settlement wallet (`WalletsService`), reads `FractionToken.balance(wallet)` for every `deployed` contract (`findAllDeployed`) in **bounded parallel** via the simulate-only **`FRACTION_READ_SERVICE`** port (`SorobanFractionReadService`; `FakeFractionReadService` in `test/shared/`), derives the artist lockup (display-only; chain enforces the real gate), and returns non-zero holdings with a derived `artworkSlug`. Per-wallet 30s Redis cache (`HoldingsCache`, fail-fast + fail-open) + in-process single-flight; any read failure -> 503 `HOLDINGS_UNAVAILABLE` (complete-or-nothing). Bounded fan-out + per-call timeout + overall budget via `fraction-read.config.ts` (no signing secret — the read source pubkey is derived from `FRACTION_RELAYER_SECRET`). The `ArtworkStatus` tuple/type lives in `fractionalization/constants/artwork-status.constant.ts` (single source of truth, re-exported by the entity). `fraction_contracts.status` is the source of truth; `artworks.status` a derived latch advanced in the same CAS txn. Reuses the shared `IdempotencyStore`, the neutral `WalletsAuditModule` (audit kinds `artwork.fractionalization.requested|deployed|failed`, actor `admin`), and `WalletsService.resolvePrimarySettlementAddress`. Migrations `1716000000027` (artworks + opt-in seed) / `1716000000028` (fraction_contracts + partial-unique-excluding-failed + soft-delete-guard trigger) / `1716000000030` (TOV-240: composite `(status, created_at DESC)` partial index for the admin-list sort, supersedes the status-only index) / `1716000000031` (TOV-237: `IDX_fc_deployed` partial index on `(artwork_id, token_address) WHERE status='deployed'` for the holdings read).
- **kyc-allowlist/** -- on-chain KYC allowlist admin domain (TOV-235 write, FR-04.MVP.03a; TOV-241 read, FR-04.MVP.03c). Provider-only + neutral (no route surface). Owns the append-only `kyc_allowlist_events` + advisory-mirror `kyc_allowlist_state` entities/repos (`findByWallet` is a non-transactional injected-repo read; `upsert` keeps its caller-`manager` path — asymmetry documented in the interface JSDoc) and the **`KYC_ALLOWLIST_TX_SERVICE`** port (`SorobanKycAllowlistService`; `FakeKycAllowlistService` in `test/shared/` for tests). Adds/removes Collector wallet addresses on the deployed KYCAllowlist contract (TOV-141): smart-wallet contract StrKeys (`C…`) **and BYOW classic account StrKeys (`G…`, TOV-243)** — the on-chain `add`/`is_allowed` + SDK `Address.fromString` take any `Address`, so the shared `walletToScVal` encoder accepts `isValidContract || isValidEd25519PublicKey` (an explicit allowlist: muxed `M…`/claimable-balance `B…`/liquidity-pool `L…` are rejected even though SDK v15 `Address.fromString` would accept them; the account arm is golden-vector-pinned separately). Allowing a `G…` lets a Collector receive FractionTokens on their own settlement wallet (unblocks TOV-33 rotation). **D7 confused-deputy guard:** an `add` of a `G…` that is not a live, SEP-10-proven `wallets` binding (advisory `WalletsService.isKnownActiveByowAddress`, fail-open, never blocks) stamps `unboundExternalWallet:true` on the batch audit row. **ONE** `KYC_ALLOWLIST_ADMIN_SECRET` is BOTH the contract admin AND the tx source (so `admin.require_auth()` is satisfied by the envelope signature — no `authorizeEntry`), on its own `relayer:kyc-allowlist:account` lock key. **Submissions are SERIALIZED, not parallel** — Soroban allows one tx per source account per ledger (see `docs/solutions/integration-issues/soroban-one-tx-per-source-account-per-ledger.md`); the adapter polls each tx to closure *inside* the lock so the next item sees the advanced sequence, and the lock TTL is derived from `submitTimeoutMs`. Only the read-only `is_allowed` checks run in parallel (capped). The HTTP surface is `backoffice/kyc-allowlist/` in `BACKOFFICE_MODULES`: **(write, TOV-235)** `POST kyc/allowlist` → 200 per-item results, `409` all-noop; `remove` requires SUPERADMIN; `add` of a `C…` smart-wallet is ADMIN+SUPERADMIN, but `add` of an external BYOW `G…` wallet requires SUPERADMIN (TOV-243 #438 — allowlisting an external wallet is custody-granting, so it sits at the reversible-`remove` bar; enforced in-service since a batch may mix kinds). **(read, TOV-241)** `GET kyc/allowlist/:wallet` → `200 { wallet, isAllowed, lastAction, lastTxHash, lastLedger, updatedAt }` (ADMIN+SUPERADMIN); mirror-only, **no on-chain call**. A never-seen wallet is `200 { isAllowed:false }` (provenance null), **NOT 404** — so the UI tells "not on the list" apart from "endpoint unavailable" (404 intentionally out of contract). `:wallet` validated by `ParseStrKeyAddressPipe` (shares `isValidStrKeyAddress` + `STRKEY_ADDRESS_MESSAGE` with the POST DTO's `IsStrKeyAddress`; accepts `G…` or `C…`, TOV-243) → `400 VALIDATION_FAILED`. Own **60/min** throttle (write is 10/min) + `Cache-Control: no-store`. `isAllowed` is advisory/display-only — **not an authz signal** (eventually-consistent; read on-chain if a trustworthy value is needed). Reuses the shared `IdempotencyStore` (batch-level key `idem:kyc-allowlist-batch:`, write only) and the neutral `WalletsAuditModule` (write: `kyc.allowlist.processed`, one row per batch keyed on `batch_id`; read: `kyc.allowlist.read`, **fail-closed** per read, `subject_id` = a v5 uuid derived from the wallet via `@common/utils/uuid-v5.util` since that column is uuid-only, raw StrKey in payload). Migrations `1716000000029` (both tables + append-only trigger) / `1716000000057` (TOV-243: widen the `CHK_kae_wallet` + `CHK_kas_wallet` wallet regex `^C…` → `^[GC]…` via DROP + ADD `NOT VALID` + VALIDATE; fail-closed VALIDATED `down()` aborts on any G-row). NB: distinct from the export module's `fraction_kyc_allowlist` (off-chain export-destination list). See `docs/solutions/code-review-resolutions/pr37-tov241-kyc-allowlist-read-review-resolutions.md`.
- **offerings/** -- primary-Offering planning domain (TOV-152, FR-05.01). Provider-only + neutral (no route surface of its own). Owns the `Offering` entity/repo (`OFFERING_REPOSITORY`; `IOfferingRepository` extends `IBaseRepository<Offering>` with `findActiveByArtworkId` — added TOV-153 for the artwork-detail `activeOffering` embed; write uniqueness stays DB-enforced), the `OFFERING_STATUSES` tuple (`planned → approved → opened → subscribed → settled` + terminal `canceled`), and the exported `ACTIVE_OFFERING_STATUSES` constant (the non-terminal set that `findActiveByArtworkId` filters on; mirrors the migration's partial-unique `WHERE`-clause and is drift-guarded against it via `pg_indexes`). The shared `offering-planning.helpers.ts` (`assertBandValid`/`resolveOfferableFloat`) is consumed by BOTH this POST service and the TOV-153 preview, so the two paths validate/compute identically; `STROOPS_RE`/`MAX_STROOPS`/`MAX_I128` live in a dependency-free `@common/constants/stroops.constant.ts` leaf (relocated from `offerings/` in TOV-172 review #351 so `marketplace` and other domains share them without a cross-domain edge) so pipe DTOs import them without the entity. The HTTP surface is `backoffice/offerings/` (`POST offerings` → 201 `planned`, `Idempotency-Key` required, `@AdminRoles(ADMIN, SUPERADMIN)`), in `BACKOFFICE_MODULES`. **Pure DB write — no Soroban, no BullMQ:** `BackofficeOfferingsService` validates the price band (`0 < low < high <= 2^96-1`, the `<= 2^96-1` check is in the SERVICE so an oversized price is a clean 422, not a 500 from `CHK_off_band`) and window (`open < close` only; a past window is intentionally allowed at planning — the M05 open/settle FRs must re-validate vs `now`), resolves the artwork's single active `deployed` fraction_contract, snapshots `public_float = total_supply − artist_retention − treasury_retention` (i128/BigInt, via `computePublicFloat` in `fractionalization/fraction-contract.helpers.ts`; rejects `<= 0` → 422 `OFFERING_NO_FLOAT`), then inserts + writes the `offering.planned` audit row in one txn. Idempotency `begin` precedes every state-derived rejection; the fingerprint **canonicalizes** timestamps (`toISOString()`) so a re-serialized-equivalent retry replays the 201 (not a false 422). One-active-offering-per-artwork is the sole authoritative guard via the `UQ_offerings_active_per_artwork` partial index + 23505 catch (no pre-check SELECT). DTO timestamps mandate an explicit tz offset (`@IsISO8601` alone does NOT). Reuses the shared `IdempotencyStore` (`idem:offering-plan:`), the neutral `WalletsAuditModule` (audit kind `offering.planned`, actor `admin`), and `FractionalizationModule` (`ARTWORK_REPOSITORY` + `FRACTION_CONTRACT_REPOSITORY`). Migration `1716000000032` (offerings table + band/float/window CHECKs + partial-unique index; `NOT NULL` on all business cols) / `1716000000033` (composite `FK_offerings_artwork_fc (artwork_id, fraction_contract_id) → fraction_contracts(artwork_id, id)` + `UQ_fc_artwork_id`, so the DB guarantees the snapshotted contract belongs to the offering's artwork). See `docs/solutions/code-review-resolutions/pr36-tov152-offering-planning-review-resolutions.md`. **TOV-154 (FR-05.02) adds the async approval + escrow-deploy surface on top of this neutral domain** (planning stays pure-DB-write; approval is the async one): `POST /offerings/:id/approve` (backoffice, `@AdminRoles(ADMIN, SUPERADMIN)` + a config roster gate — the roster, not the role, is the gate) enforces an **app-level 2-of-3 quorum** via a new append-only `offering_approvals` table (partial-unique `(offering_id, admin_sub) WHERE deleted_at IS NULL`; `insertSignature` uses `ON CONFLICT DO NOTHING`, NOT a caught 23505, because a caught 23505 aborts the surrounding txn). Reaching quorum CAS-claims the deploy (`escrow_deploy_status: NULL|failed → deploying`, the enqueue-once guard under `SELECT … FOR UPDATE`) and best-effort-enqueues a BullMQ job; a concurrency:1 worker deploys the per-offering `OfferingEscrow` Soroban contract via the `OFFERING_ESCROW_SERVICE` port (`SorobanOfferingEscrowService` in the **worker** module `deploy/offering-worker.module.ts`, imported into `app.module` — NOT the neutral module, so the fixed-config integration harness can still import `OfferingsModule`; `Operation.createCustomContract`, admin-as-source, deterministic address, `getLedgerEntries` self-heal with a WASM-hash gate, fee cap, lock TTL derived from real RPC bounds). Success latches `escrow_deploy_status→deployed` + `status planned→approved` in one CAS txn (**dual-latch**: escrow_deploy_status = source of truth, offerings.status = derived latch). The reconcile processor (`OfferingReconcileProcessor`, repeatable cron, DB-only) runs THREE sweeps: **stale-deploying re-drive** (`findStaleDeploying` → re-enqueue; the P1 backstop for a crash between commit and enqueue — expiry excludes mid-deploy rows), **window-open** (`casOpened`, guarded `window_open_at<=now() AND window_close_at>now()` so an elapsed window isn't auto-opened), and **7-day approval expiry** (attempt-window anchored at first signature). Reads: `GET /offerings` (paginated approval work-queue, roster-intersected `count` + per-caller `youApproved`, index `IDX_offerings_list`) + `GET /offerings/:id` (202-poll target) — both project `OfferingDetailDto` (the list omits `signers`). Approver attests to the frozen `snapshot_artist_address` (money-routing), re-asserted before deploy. Config `offering-escrow.config.ts` (roster/threshold + a floor-of-2 assertion, non-enumerable admin secret, WASM hash lowercased, own lock key `relayer:offering-escrow:account`). Audit kinds `offering.approval.signed|expired`, `offering.escrow.deployed|deploy_failed`, `offering.opened`. Migrations `1716000000034` (offering_approvals + escrow columns + broadened `CHK_off_approved_has_escrow` + append-only trigger) / `1716000000035` (list index). See `docs/solutions/code-review-resolutions/pr39-tov154-offering-multisig-approval-escrow-deploy-review-resolutions.md`. **TOV-156 (FR-05.03) adds the public passkey-signed bid surface on top of this neutral domain** (the `OfferingBid` entity + `OFFERING_BID_REPOSITORY` are folded INTO `OfferingsModule`, mirroring `OfferingApproval`). The **`bids/` subtree**: a public authenticated surface `PublicOfferingBidsModule` (in `PUBLIC_MODULES`) → `OfferingBidsController` on `@Controller('offerings')` — `POST :id/bids/prepare` (validate band/whitelist/window/status + a USDC balance pre-check, then `relayer.buildBid` → an unsigned `submit_bid` tx + WebAuthn challenge; **no DB write**), `POST :id/bids` (record a `submitted` bid + enqueue the async escrow; `Idempotency-Key` required; **201**), `GET :id/bids/me` (the caller's single active bid, the `submitted → escrowed|failed` poll target). A bid escrows `price × count` USDC into the offering's `OfferingEscrow` via the on-chain **`submit_bid(bidder, price, count, idempotency_key)`** — which `bidder.require_auth()`s + `usdc.transfer(bidder → escrow)`, so only the collector's **passkey** can move the funds (two-call flow: prepare → sign → submit). The relayer port gained `buildBid`/`submitSignedBid`/`assertBidNotExpired` + a **forked `verifyBidAuthorization`** (`bid-authorization.ts`): `submit_bid`'s auth tree is the root with the inner `usdc.transfer` as a **sub-invocation** (the audited TOV-22 flat-transfer verifier bans sub-invocations), and it EXACT-pins the escrow contract, bidder, price, count, the inner amount (= price×count), and the on-chain `idempotency_key` to server-trusted values (rejects `SOURCE_ACCOUNT` creds). The **`bids/escrow/` subtree** is the provider-only worker (`OfferingBidWorkerModule`, imported in `app.module`): `OfferingBidEscrowProcessor` (`concurrency:1` on the shared relayer account) relays the signed `submit_bid` and CAS-latches `submitted → escrowed` (stamps `chain_bid_id` + tx hash) / `failed`. **Money-safety:** the worker CAS-`failed`s ONLY on provably-no-funds-moved outcomes (`isProvablyNoFundsMoved`, `attemptsMade`-aware) — a lost-confirmation/post-send-ambiguous error stays `submitted` so the one-active-bid slot is never wrongly freed (no double-escrow). There is intentionally **no bid reconciler** (the safe DB↔chain adopt-by-`DuplicateBid`-query is a live-testnet-gated follow-up); stranded `submitted` bids are resolved manually via the stuck-bid monitoring alert. End-to-end idempotency: HTTP `Idempotency-Key` → on-chain `BytesN<32>` = `sha256(offeringId|collectorSub|key)` baked into the signed tx → DB `idempotency_hash` + partial-unique `UQ_offering_bids_idem`. Reuses the shared `IdempotencyStore` (`idem:offering-bid:`), the neutral `WalletsAuditModule` (kinds `offering.bid.submitted|escrowed|escrow_failed`), `WalletsService.resolveEmbeddedWalletForUser`, and `UserRepository.findKycStatusByUserId` (the whitelist gate). Migration `1716000000036` (`offering_bids` money table: STORED generated `escrow_amount_stroops`, forward-only append-only-ish guard trigger, partial-unique active-per-collector + idem indexes, `chain_bid_id bigint` for the contract u32, fail-closed `down()`). Deploy runbook: `docs/solutions/deployment-issues/2026-08-20-tov156-offering-bids-deploy-runbook.md`. Review resolutions: `docs/solutions/code-review-resolutions/pr41-tov156-bid-submission-review-resolutions.md`. **TOV-158 (FR-05.04) adds the cancel/refund path on the SAME `bids/` subtree** (two new routes on `OfferingBidsController`: `POST :id/bids/cancel/prepare` [no body, **no Idempotency-Key** — `cancel_bid` has no on-chain idem arg] + `POST :id/bids/cancel` [**202**, Idempotency-Key required]). A bid refunds via the on-chain **`cancel_bid(caller, bid_id)`** (`caller.require_auth()` + `usdc.transfer(escrow → bidder)`); the caller's auth tree is **root-only** (the refund `from` is the contract), so the relayer gained `buildCancelBid`/`submitSignedCancelBid` + a forked `verifyCancelBidAuthorization` (`cancel-authorization.ts`) that exact-pins escrow/caller/`bid_id` [u32-native `requireU32Equal`, NOT the i128 helper] and asserts **zero sub-invocations**; expiry re-check REUSES `assertBidNotExpired` (fn-agnostic). New states `canceling`(active)→`canceled`(frees slot) via `casCanceling`/`casCanceled`/`casCancelFailedBackToEscrowed`; the **`bids/cancel/` worker** (`OfferingBidCancelWorkerModule`, in `app.module`) runs the **INVERTED** money-safety classifier — `canceling→canceled` on a provably-landed refund, `canceling→escrowed` on provably-no-refund (`isProvablyRefundNotSent`), **ambiguous STAYS `canceling`** (never revert-after-landed = no double-refund; `pollForCancel` timeout→`unavailable` NOT `transfer_failed` is the merge-gate). No reconciler: a stuck `canceling` self-heals once the signature expires (retry budget outlasts `BID_SIG_VALIDITY_LEDGERS`)→`expired`→`canceling→escrowed`; enqueue partial-failure COMPENSATES (revert + `idempotency.fail` + 503). Cancel is **NOT whitelist-gated** (a de-whitelisted collector must still reclaim). `GET :id/bids/me` now reads `findMyLatestBid` (incl. terminal `canceled`, surfacing `refundTxHash`/`canceledAt`). Both bid workers share ONE `relayer:account` lock → ~0.2 tx/s is a JOINT SLO. Migration `1716000000037` (widen `CHK_bid_status` via `NOT VALID`/`VALIDATE`, `CHK_bid_refund_clean`, write-once `refund_tx_hash`/`canceled_at`, forward-only trigger `escrowed→canceling→canceled`+`canceling→escrowed`, split index predicates [active belt frees on `canceled`; idem belt KEEPS `canceled` to mirror the permanent on-chain Idem key], plain `IDX_offering_bids_collector` btree). Audit kinds `offering.bid.canceling|canceled|cancel_failed`. FE contract: `docs/api-contracts/2026-08-20-tov158-bid-cancel-api-contract.md`. Review resolutions: `docs/solutions/code-review-resolutions/pr42-tov158-cancel-refund-review-resolutions.md` (7-agent review, 9 findings). NB post-review: (1) the `cancel()` producer compensates ONLY on `cancelQueue.add` failure — `idempotency.complete()` is best-effort OUTSIDE the enqueue try (a post-enqueue blip must never revert a LIVE refund job → double-refund); (2) `GET :id/bids/me` poll target `findMyLatestBid` is index-served by `IDX_offering_bids_collector (collector_sub, offering_id, created_at DESC, id DESC)` (no Sort); (3) cancel job `attempts:12` (~3 attempts past sig-expiry for the self-heal); (4) `mapCancelRelayerError`/`mapRelayerError` share `mapRelayerErrorTo`. **TOV-160 (FR-05.05) adds the uniform-price clearing + async settlement pipeline.** The **`settle/` subtree** is the provider-only worker (`OfferingSettleWorkerModule`, in `app.module`; binds `OFFERING_ESCROW_SERVICE` in the WORKER module — the neutral `OfferingsModule` stays config-free): `OfferingSettleProcessor` (`concurrency:1`, shared `relayer:offering-escrow:account` lock) is **self-heal-first** — it reads on-chain `status()` at the top of every attempt (settlement is on-chain-idempotent via the contract's CEI `AlreadySettled` gate), so `Settled`→adopt, `Open`→`close_offering` then settle, `Closed`→settle; then it recomputes the clearing from the `escrowed` book, calls `close_and_settle(clearing_price, Vec<(u32,i128)>)`, and CAS-flips `subscribed→settled` + the bid **`won`/`lost`** terminal states + the `offering_clearing_audit` snapshot ALL in one txn (F1: `insertSnapshot` asserts exactly 1 row via `.orIgnore()`→throw, so a stale snapshot rolls the whole settle back). **Money-safety classifier (`isTerminalSettleError`)**: only DETERMINISTIC failures (an `OfferingEscrowError` the adapter marked non-retryable — incl. `OfferingSettleContractError`; a `RangeError` from `assertClearingInvariants`; a persist-time `SettleInvariantError`) stamp a terminal `settle_failed_at` (offering stays `subscribed`, excluded from the sweep, admin re-drives with a FRESH key); everything else (transient DB / RPC / a duplicate-job `OfferingNotOpen`) re-throws RETRYABLE so the next attempt's self-heal adopts (never a spurious wedge). The **pure `clearing.ts`** at the offerings ROOT (like `offering-planning.helpers`, consumed by BOTH the worker and the backoffice preview) does the BigInt walk (`price DESC, created_at ASC, chainBidId ASC` — the algorithm's authoritative tiebreak since TOV-162; the DB scan stays `id ASC` as a stable-scan detail and the walk re-sorts) → P = marginal winner price, and the marginal tier is allocated pro-rata (see the TOV-162 clause below — TOV-160 originally single-marginal-partial-filled); `assertClearingInvariants` is the fail-fast belt (band, `Σ allocated == public_float`, i128/`MAX_I128` overflow, **and an INDEPENDENT optimality belt: P == min winner price + every loser ≤ P**, because the contract is a conservation belt NOT a correctness oracle). The `settle-args.ts` encoder (under `escrow/`, golden-vector-pinned) builds `Vec<(u32,i128)>` as an outer `scvVec` of inner `[scvU32, scvI128]` tuples + validates `chain_bid_id ∈ [1,2^32-1]` before signing. The escrow port gained `closeOffering`/`closeAndSettle`/`readStatus` (admin-as-source, no `authorizeEntry`, auth-entry hard-reject). HTTP: `backoffice/offerings/` `POST :id/settle` (202, Idempotency-Key, preconditions, CAS-latch `opened→subscribed`, complete-before-best-effort-enqueue) + `GET :id/clearing-preview` (window-closed-gated dry-run, identity-redacted, best-effort access-audit) live in a SPLIT `BackofficeOfferingSettleService` (extracted from the planning/approval god-service). The stale-`subscribed` reconcile is a SEPARATE queue/scheduler/processor (own `OFFERING_SETTLE_RECONCILE_ENABLED` toggle) — the crash-between-commit-and-enqueue backstop. **`MAX_BIDS_PER_OFFERING`** (one shared `OFFERING_MAX_BIDS_DEFAULT`, both configs) is enforced ATOMICALLY at bid submit via a per-offering `pg_advisory_xact_lock` (the on-chain `close_and_settle` refunds EVERY active bid in one tx → a book over the ceiling is unsettleable). Migrations `1716000000038` (transactional: `offering_clearing_audit` FULLY-immutable append-only table [trigger rejects soft-delete] + FK + i128 aggregate CHECKs with `div()` floor-split matching the contract; `offering_bids` `won`/`lost` states + `allocated_count`/`settle_refund_stroops` write-once stamps + guard-trigger update; `offerings` `settle_failed_at`/`settle_failure_reason`) + `1716000000039` (`transaction:false` `CREATE INDEX CONCURRENTLY IDX_offering_bids_clearing`, `RESET lock_timeout` first — a plain `SET` from prior migrations leaks across TypeORM's shared `migration:run` connection). Audit kinds `offering.clearing.previewed|closed|settled|settle_failed`. Deploy runbook: `docs/solutions/deployment-issues/2026-08-20-tov160-settlement-deploy-runbook.md`. Review resolutions (8-agent, NO P1, 23 fixed): `docs/solutions/code-review-resolutions/pr43-tov160-clearing-settlement-review-resolutions.md`. Real on-chain settlement is **chain-gated** (needs `bind_token`/token-minter-=-escrow/9-arg WASM); ships Fake-adapter-proven, `OFFERING_TOKEN_NOT_BOUND` terminal until then. **TOV-162 (FR-05.05b) changes ONLY the marginal-tier allocation inside the pure `clearing.ts`**: when demand at exactly P exceeds the float left after all strictly-above-P bids fill, the `== P` bidders share the remainder **pro-rata** (`floor(count × remainingFloat / totalAtClearing)`, Hamilton/largest-remainder) instead of TOV-160's single-marginal-partial-fill; the integer remainder ("dust", `0…N−1`) is handed out `+1` each FCFS by `(created_at, chainBidId)`, and a `== P` bidder whose share floors to 0 with no dust is excluded from winners and fully refunded as `lost`. `Σ allocated == public_float` still holds exactly → **no migration/contract/endpoint change** (audit `proceeds`/`fee`/`net` are byte-identical since `proceeds = P·float` regardless of the split). The sort/dust tiebreak moved from the UUID `id` to **`chainBidId`** (D4′) because `id` is absent from `bids_snapshot`, so an `id`-based belt could not verify dust ordering; the DB `id ASC` scan order is retained as a stable-scan detail (the algorithm re-sorts). A new **INDEPENDENT pro-rata belt** in `assertClearingInvariants` re-derives the tier split from the frozen snapshot (its OWN `(created_at, chainBidId)` sort, not the production path) and throws terminal `RangeError` on any deviation (above-P full-fill; each `==P` winner ∈ `[floor(base), floor(base)+1]`; the `+1` recipients are the earliest by `(created_at, chainBidId)`). PR #44; plan `docs/plans/2026-08-21-feat-oversubscription-pro-rata-clearing-plan.md`; deploy-drain guard = runbook §7; review resolutions (6-agent, NO P1/P2 defects): `docs/solutions/code-review-resolutions/pr44-tov162-oversubscription-pro-rata-review-resolutions.md`. **TOV-165 (FR-05.06) adds settlement audit fidelity + a derived `settlementPhase`** on top of the shipped pipeline (reconcile-deltas-only — the state-machine job, `close_and_settle`, atomic audit, and admin re-trigger already shipped in TOV-160/162). (1) The **offering** now snapshots `total_supply_stroops`/`artist_retention_stroops`/`treasury_retention_stroops` at planning (in `BackofficeOfferingsService.create`, from `resolveOfferableFloat`), frozen beside `public_float` like `snapshot_artist_address` — `CHK_off_public_float_decomposition` (`public_float = total_supply − artist − treasury`) enforces consistency, so settlement never re-reads the mutable `fraction_contracts`. (2) `offering_clearing_audit` gains `cleared_allocations_stroops` (the INDEPENDENT Σ of winner allocations, not an echo of `public_float`), `absorbed_leftover_stroops` (≡ 0 today, FR-04.06 placeholder), and the three copied supply/retention columns — the settle worker's `persist()` copies them off the offering (both normal + adopt paths funnel through the one `insertSnapshot`, no cross-domain read, no NULL guard) so the audit row is **self-contained**. Three INDEPENDENT teeth-bearing CHECKs whose conjunction implies the mint invariant `Σ allocated + artist_retention + treasury_retention + absorbed_leftover == total_supply`: `CHK_clearing_float_decomposition`, `CHK_clearing_alloc_eq_float`, `CHK_clearing_absorbed_zero` (the original TOV-165 "mint invariant CHECK" was dropped as tautological — `absorbed` is a residual of the other four). (3) A pure, clock-injected `deriveSettlementPhase(o, now)` (`backoffice/offerings/settlement-phase.ts`, single consumer = the read DTO) surfaces the FR-05.06 vocabulary `closed|settling|settled|canceled|null` on `OfferingDetailDto` via an exhaustive `switch`+`assertNever` (status-first; window consulted only in the `opened` arm; the service passes one `now` per response page; failed settle stays `'settling'` with `settleFailedAt` alongside; undersubscribed rests at `'closed'`). Migration `1716000000040` (transactional, two-part, `SET LOCAL lock_timeout`): backfill BEFORE the append-only guard re-declaration (else self-lockout), NULL-safe pre-VALIDATE assertions. Plan: `docs/plans/2026-08-21-feat-tov165-settlement-audit-fidelity-plan.md`; runbook §8; PR #45; review resolutions (7-agent, NO P1/P2 defects; 1 P2 docs + 4 P3, all fixed): `docs/solutions/code-review-resolutions/pr45-tov165-settlement-audit-fidelity-review-resolutions.md`. The offering-inserts seed helper `test/shared/seed-offering.ts` (`insertOffering`) centralizes the offerings INSERT column list across integration/e2e specs (except `offerings.constraints` — subject-under-test).
- **marketplace/** -- secondary-marketplace RFQ domain (TOV-172, FR-06.01; M06). The **`rfqs/` subtree** splits neutral vs public like offerings: a config-free neutral **`RfqsModule`** owns the `Rfq` entity + `RFQ_REPOSITORY` (repo `insertOpen` = `.orIgnore('("collector_sub","idempotency_key_hash")')`→null→replay, NOT a caught 23505; `countActiveOpenByCollector`), and a public authenticated surface **`PublicRfqsModule`** (in `PUBLIC_MODULES`) → `RfqsController` on `@Controller('marketplace/rfqs')` — `POST /api/v1/marketplace/rfqs` (whitelisted Collector issues an RFQ on a fractionalized artwork; `Idempotency-Key` required; **201**; throttle 20/60s). **Pure DB write — no Soroban tx, no BullMQ.** `RfqsService.create`: idempotency `begin` precedes EVERY state gate (whitelist → active-open ceiling → price/overflow → artwork → fractionalized) so a legit retry replays the original 201 even after de-whitelisting; whitelist = `users.kyc_status == 'whitelisted'` (`findKycStatusByUserId`, 403 `RFQ_NOT_WHITELISTED`); fractionalized = a `deployed` `fraction_contracts` row (`findActiveByArtworkId`, 422 `RFQ_ARTWORK_NOT_FRACTIONALIZED`; missing artwork 404 `ARTWORK_NOT_FOUND`); price bounds `1..2^96-1` (422 `RFQ_INVALID_PRICE`) + aggregate `price×count <= MAX_I128` (422 `RFQ_AMOUNT_OVERFLOW`); per-collector active-open ceiling `MAX_ACTIVE_OPEN_RFQS=25` (422 `RFQ_TOO_MANY_ACTIVE`, served by the partial `IDX_rfqs_active_open`). `expiry_hours` optional, default 48h; store only the derived `expires_at`. **Soft-warn balance** via the extracted **`RfqBalanceAdvisor`** (`relayer.readWalletHoldings([usdcTokenAddress])`, wrapped in a ~1.2s `withRpcTimeout` deadline) — NEVER throws/blocks: no wallet / RPC fail / timeout → 201 with no `balanceWarning`; the warning is a FRESH-response-only concern (the idempotency snapshot is stored warning-free so Redis + DB-backstop replays return identical bodies). **Idempotency**: shared `IdempotencyStore` (`idem:rfq:${userId}:${key}`, canonicalized fingerprint) + a **durable** full-unique `UQ_rfqs_idem (collector_sub, idempotency_key_hash)` backstop closing the commit→complete crash window (`.orIgnore()`→replay). Fail-guard mirrors `OfferingBidsService.submit` (`fail()` pre-commit only, `complete()` outside). Reuses the neutral `WalletsAuditModule` (audit kind `rfq.created`, actor `user`, in the insert txn) + `WalletsService.resolveEmbeddedWalletForUser`. DTOs are **camelCase** (matches `/bids`, `/me/holdings`; the FR-06.01 ticket's snake_case body was deliberately overridden in PR #46 review for api/v1 consistency). Migration `1716000000041` (`rfqs` table: `numeric(39,0)` money, `CHK` belts, **composite FK** `(artwork_id, fraction_contract_id) → fraction_contracts(artwork_id, id)` reusing the existing `UQ_fc_artwork_id` from `033` [no single-column artwork FK — redundant], FULL-unique `UQ_rfqs_idem`, `IDX_rfqs_active_open`, `fn_rfqs_guard` guard trigger = block delete/soft-delete + freeze money cols + FORWARD-ONLY status `open→filled|canceled|expired`, `SET LOCAL lock_timeout`, fail-closed `down()`). Plan `docs/plans/2026-08-21-feat-rfq-creation-endpoint-plan.md`; FE contract `docs/api-contracts/TOV-172-rfq-create.md`; deploy runbook `docs/solutions/deployment-issues/2026-08-21-tov172-rfq-creation-deploy-runbook.md`; review resolutions (9-agent, NO P1/P2 defects; 7 P2 + 5 P3, all fixed per-issue): `docs/solutions/code-review-resolutions/pr46-tov172-rfq-creation-review-resolutions.md`. Downstream invariant: FR-06.03 accept-and-settle MUST re-verify the creator's whitelist at settle time (an `open` RFQ can outlive a freeze; no sweeper in scope). Shared money constants (`STROOPS_RE`/`MAX_STROOPS`/`MAX_I128`) were relocated `offerings/` → `@common/constants/stroops.constant` in this PR. **TOV-174 (FR-06.02) adds the notification fan-out on RFQ create** in a sibling `notifications/` subtree (neutral/worker/surface triple): on create, `RfqsService` best-effort-enqueues a `rfq-fanout` BullMQ job (post-`complete()`, `jobId=rfqId`, `PublicRfqsModule` registers the queue — the neutral `RfqsModule` stays queue-free); a latch-gated worker (`RfqFanoutWorkerModule`, in `app.module`) resolves DISTINCT settled-offering **winners** for the artwork (`OfferingBidRepository.findSettledWinnerSubsForArtwork`, joins `offerings` — bids have no artwork_id — `status='won'`, excludes the buyer) and, in one txn, UNNEST-inserts one `rfq_notifications` row per recipient (`ON CONFLICT (rfq_id,recipient_sub,channel) DO NOTHING`), CAS-stamps the write-once `rfqs.fanned_out_at` latch, and — **gated on the CAS win** so concurrent workers never double-audit — writes `rfq.notifications.fanned_out`. Winners == holders only until secondary transfers ship (Risk U1; `fraction_holders` projection is the future fix). A repeatable `RfqFanoutReconcileScheduler`/`…Processor` re-drives un-latched RFQs (`findUnfannedSince`, served by partial `IDX_rfqs_unfanned`) — the backstop upholding the 99%/60s SLA. It re-enqueues with a **unique jobId** (`${rfqId}:reconcile:${ts}`, NOT `jobId=rfqId`) so it recovers BOTH the commit→enqueue crash gap AND retry-exhausted jobs (a `jobId=rfqId` re-add dedups to a no-op against a retained failed job — PR #47 #361); a **recency grace** (`reconcileGraceMs`, floor 90s > the ~62s retry horizon) bounds it to `[now−windowMs, now−graceMs)` so it never redundantly re-drives a still-running job. The DB latch + UQ keep re-drives idempotent (jobId is NOT the correctness boundary). Read surface `PublicMeNotificationsModule` (in `PUBLIC_MODULES`) → `MeNotificationsController` on `@Controller('me/notifications')`: owner-scoped `GET /me/notifications` (paginated, `?filter=unread`, single JOIN to rfqs+artworks, `artworkSlug` derived via the shared `@common/utils/slug.util` helper, **never** exposes the buyer's `collector_sub`, DOES expose `maxPricePerFractionStroops`), `GET /me/notifications/unread-count` (bounded LIMIT-100 probe → "99+"), `PATCH :id/read` (set-once; 404 `NOTIFICATION_NOT_FOUND` no-oracle; not whitelist-gated per TOV-158). The neutral `RfqNotificationsModule` is entity+repo only (keeps `OfferingsModule` off the read path). Migrations `1716000000042` (rfq_notifications + FULL-unique `UQ_rfq_notifications_recipient_channel` [NOT partial — a bare ON CONFLICT can't infer a partial index] + inbox/unread partial indexes + `fn_rfq_notifications_guard` [block delete/soft-delete + freeze identity cols; read_at NOT frozen] + `rfqs.fanned_out_at` + re-declared `fn_rfqs_guard` with NULL-safe write-once + backfill `= created_at` + `IDX_rfqs_unfanned`) / `1716000000043` (`transaction:false` CONCURRENTLY `IDX_offerings_artwork` + `IDX_offering_bids_won_recipients`). Plan `docs/plans/2026-08-21-feat-rfq-notification-fanout-plan.md`; FE contract `docs/api-contracts/2026-08-21-tov174-rfq-notifications-api-contract.md`; deploy runbook `docs/solutions/deployment-issues/2026-08-21-tov174-rfq-notification-fanout-deploy-runbook.md`; review resolutions (9-agent, NO P1; 1 HIGH-reliability + 1 contract P2 + 7 P3, all fixed per-issue) `docs/solutions/code-review-resolutions/pr47-tov174-rfq-notification-fanout-review-resolutions.md` + the reusable BullMQ lesson `docs/solutions/integration-issues/bullmq-reconcile-jobid-collision-with-retained-failed-job.md`. **TOV-175 (FR-06.03) adds the quote-submission surface** in a sibling `quotes/` subtree (neutral `QuotesModule` + public `PublicQuotesModule` in `PUBLIC_MODULES`) → `QuotesController` on `@Controller('marketplace/rfqs')` (shares the RFQ base path, no collision) — `POST /api/v1/marketplace/rfqs/:id/quotes` (whitelisted holder offers to SELL fractions on an open RFQ; `Idempotency-Key` required; **201**; throttle 20/60s). **Pure DB write at submit, gated by a HARD, fail-closed on-chain free-balance check** (unlike TOV-172's soft warn): `free = onchainBalance − Σ(holder's other open, non-expired quotes' count on the same token)`, read via the neutral **`FRACTION_READ_SERVICE`** (relocated to the `fractionalization/` root in this PR) off the holder's **embedded passkey wallet** (`resolveEmbeddedWalletForUser`, aligns with bids + the FR-06.04 sell source); read fail/timeout → 503 `QUOTE_BALANCE_UNAVAILABLE`, `free < count` → 422 `QUOTE_INSUFFICIENT_FREE_BALANCE` (body adds `requiredCount`/`freeBalance`). The free decision + active-per-RFQ check run INSIDE a per-`(holder, fraction_contract)` `pg_advisory_xact_lock`; an in-txn **lazy-reap** flips the holder's own lapsed quotes to `expired` (no sweeper). One open quote per (rfq, holder); `validUntil` absolute ISO-8601 silently capped to the RFQ expiry (`validUntilCapped`). Migration `1716000000044` (`rfq_quotes`: `numeric(39,0)` money, `CHK` belts, composite anti-drift FK `(rfq_id, fraction_contract_id) → rfqs(id, fraction_contract_id)` [adds `UQ_rfqs_id_fc`], FULL-unique `UQ_quotes_idem`, partial-unique `UQ_quotes_active_per_rfq`, `IDX_quotes_locked`, `fn_quotes_guard` forward-only `open→accepted|canceled|expired`, FK-safe fail-closed `down()`). Reuses the shared `IdempotencyStore` (`idem:quote:${userId}:${rfqId}:${key}`) + `WalletsAuditModule` (kind `quote.submitted`, actor `user`). Plan `docs/plans/2026-08-21-feat-quote-submission-endpoint-plan.md`; FE contract `docs/api-contracts/2026-08-21-tov175-quote-submission-api-contract.md`; PR #48; review todos `todos/37*-*-quote-*.md`. **TOV-177 (FR-06.04+06.05) adds quote acceptance + atomic on-chain settlement** — the FIRST marketplace on-chain leg — in a sibling **`settlement/` subtree** (neutral `SettlementModule` owns the `SecondaryTrade` entity + `SECONDARY_TRADE_REPOSITORY`; public `PublicSettlementModule` in `PUBLIC_MODULES`; provider-only `QuoteSettleWorkerModule` in `app.module`). The deployed `MarketplaceSettler.accept_quote` needs BOTH parties' passkey signatures and the seller is offline at accept, so the flow is 3 steps: **`settlement/accept/authorize.*`** (seller pre-signs the 5-tuple; the signed `SorobanAuthorizationEntry` XDR is stored on the quote via new `rfq_quotes` cols `seller_auth_entry`/`seller_auth_expires_ledger`/`seller_wallet_contract`/`authorized_at` + `status_reason`, replayed at accept; `POST :id/quotes/:quoteId/authorize(/prepare)`, over-auth free-balance gate under a per-`(seller, token)` `pg_advisory_xact_lock`, `sumAuthorizedLockedCount` excludes the target quote #385) → **`settlement/accept/accept.*`** (buyer signs; `POST :id/accept(/prepare)` records a `pending` `secondary_trades` row [the per-RFQ double-accept latch `UQ_secondary_trades_pending`] under `pg_advisory_xact_lock(rfqId)` + best-effort enqueue; `GET :id/accept/me` poll; the buyer NEVER receives the seller's signed entry — combined server-side at submit) → **`settlement/settle/`** worker (`QuoteSettleProcessor` `concurrency:1`, `is_settled`-self-heal-FIRST via `MARKETPLACE_SETTLER_READ_SERVICE` [simulate-only, THROWS on unavailable — never coerce to false], relays the two-signature tx, then the shared **`SettlePersistenceService.persistSettled`** flips `trade→settled` + `rfq→filled` [reuses `filled`] + winning `quote→accepted` + rivals `→superseded` + audits in ONE atomic txn — a partial commit would open a **double-settle window** since accept gates on `rfq.status='open'`, #384). **Money-safety classifier** `settle-failure.classifier.ts` is fail-closed; a deterministic `REVERTED` with an unrecognized/`null` code terminates `keepOpen` (never expire the seller for a cross-contract [USDC-SAC vs OZ] code, #383; settler-side error-origin disambiguation is a pre-mainnet follow-up). The **reconcile FLOOR** (`settle/quote-settle-reconcile.{scheduler,processor}`, #382) sweeps stale `pending` trades: adopt-if-`is_settled` / abandon-past-BullMQ-retry-horizon (`settle_abandoned`, keepOpen — frees the latch); it CANNOT re-enqueue (buyer assertion is job-only bearer material). Relayer gained the two-signature `buildSellerQuoteAuth`/`attachSellerQuoteAuth`/`buildAcceptQuote`/`submitSignedAcceptQuote` + a forked two-entry verifier `accept-authorization.ts` (exactly 2 address-cred entries, each root 5-tuple pinned SEPARATELY since `require_auth_for_args` ≠ the op's 7 args, low-S WebAuthn, rejects SOURCE_ACCOUNT) + golden-pinned encoders `accept-quote-invocation.ts` (150/150 bps split); an OPTIONAL dedicated `RELAYER_MARKETPLACE_SETTLEMENT_SECRET` source/lock decouples settlement throughput (#386, else shared account + shared lock). Buyer read surface `settlement/../rfqs/detail/` → `GET /marketplace/rfqs/:id` (owner-scoped, open quotes price-ASC with derived `acceptable`, 404-no-oracle) unblocks FE TOV-178. Config `marketplace-settlement.config.ts` (Joi-validated #387); migration `1716000000045` (`secondary_trades` [STORED `gross_stroops`, i128 CHECK, composite FKs, pending latch, write-once guard] + `rfq_quotes` seller-auth cols + widened `CHK_quotes_status` +`superseded` + re-declared `fn_quotes_guard`; ACCESS EXCLUSIVE holds to COMMIT + blocks reads — pre-deploy row-count gate #388). Audit kinds `quote.authorized|accepted|superseded|expired`, `trade.settled|failed`. Plan `docs/plans/2026-08-22-feat-quote-acceptance-atomic-settlement-plan.md`; FE contract `docs/api-contracts/2026-08-22-tov177-quote-acceptance-api-contract.md`; PR #49; review resolutions (7-agent, all 6 money-safety invariants verified holding, 2 P1/5 P2/4 P3 all fixed per-issue) `docs/solutions/code-review-resolutions/pr49-tov177-quote-acceptance-atomic-settlement-review-resolutions.md` + the reusable lesson `docs/solutions/integration-issues/async-onchain-settlement-atomic-persist-and-reconcile-floor.md`; per-finding todos `todos/38*-*` / `todos/39*-*`.
- **health/** -- liveness/readiness probe via @nestjs/terminus [`api/v1/health`]
- **stages/**, **submissions/** -- public user-facing surfaces [`api/v1`]
- **artworks/** -- anonymous (`@Public()`) public artwork browse + detail [`api/v1/artworks`] (TOV-189, FR-08.01). **Now DB-backed** (the in-memory mock was removed): `ArtworkReadRepository` (COMPOSES `DataSource` + two `Repository<T>`, does NOT `extends BaseRepository` — the projection returns are incompatible with the entity-returning base) reads the fractionalization-owned `Artwork` + net-new `ArtworkImage` entities behind the `ARTWORK_READ_REPOSITORY` token. `ArtworksModule` registers those entities via `TypeOrmModule.forFeature` DELIBERATELY (not by importing `FractionalizationModule`) to keep the Soroban `FRACTION_FACTORY_SERVICE` bootstrap probe out of this anonymous public graph, and imports `StorageModule` for signing. **Visibility is single-sourced** in `constants/artwork-visibility.constant.ts` (`PUBLIC_VISIBLE_STATUSES = ['verified','fractionalized'] as const satisfies readonly ArtworkStatus[]` — compile-time drift guard vs the canonical union + a fail-loud `assertVisibleStatus`); list + detail both filter on it (+ `deleted_at IS NULL`), so a `published`/`fractionalizing`/soft-deleted/unknown/non-UUID id all return an identical `404 ARTWORK_NOT_FOUND` (non-oracle). The service does a **UUID guard BEFORE the DB read** (a non-UUID would otherwise hit a uuid column → Postgres `22P02` → 500). `GET /artworks/:id` returns `ArtworkDetailResponseDto` (metadata + `supportingImages: string[]` [pre-sorted signed URLs] + `coaSignedUrl` [1h] + `custodian` [public display label]); `GET /artworks` returns base fields only (no per-row signing). **Signing is BATCHED** (`StorageService.createTemporaryUrls` → one Supabase `createSignedUrls` call for all images + COA, TTL = `filesConfig.signedUrlTtl` default 3600s; PR #50 #393 — was up to N+1 per-path calls) and runs in the SERVICE, **fail-open per asset + per-batch 800ms timeout** (a failed/timed-out image is omitted, COA → null; raw storage paths never appear in the response — the DTO maps field-by-field, never spreading the record; fail-open logs carry the artwork id). Detail is `Cache-Control: no-store` (signed capability URLs must not be shared-cached) + a dedicated `@Throttle(20/min)`; list is `public, max-age=60` and NEVER signs (primary images are passthrough absolute URLs). Migration `1716000000046` (nullable `artworks.custodian` + `artworks.coa_storage_path`; `artwork_images` table [FK CASCADE — note CASCADE does NOT propagate a parent SOFT-delete; the future admin upload path must; `storage_path` non-empty + `sort_order >= 0` CHECKs, partial index `(artwork_id, sort_order) WHERE deleted_at IS NULL`]; list ordering reuses the `(status, created_at DESC)` index from …030). Read-only ticket — no upload path (COA/images/custodian are seeded; admin write is a follow-up, see todo 394). Test seeders: `test/shared/seed-artwork.ts` (`insertArtwork`/`insertArtworkArtist`) + `test/shared/fake-storage.ts` (`FakeStorageService`). FE contract `docs/api-contracts/2026-08-24-tov189-artwork-detail-api-contract.md`; reusable pattern `docs/solutions/code-review-patterns/nestjs-public-endpoint-signed-cdn-urls.md`; deploy note `docs/solutions/deployment-issues/2026-08-24-tov189-artwork-detail-deploy-note.md`; PR #50 review resolutions `todos/393-398-*`.
- **timeline/** -- public anonymous artwork provenance timeline (TOV-191, FR-08.02/08.03, M08). Neutral/public split like offerings/marketplace: the neutral **`TimelineModule`** (no controller) owns the `ArtworkTimelineEvent` entity, the read repo (`TIMELINE_READ_REPOSITORY`, composes `DataSource` + raw keyset SQL like `ARTWORK_READ_REPOSITORY`), and the best-effort **`TimelineEmitService`** (concrete, modeled on `AuditLogService` — NOT a port); it exports the emit service and is imported by the two write-domain workers (`FractionDeployWorkerModule`, `QuoteSettleWorkerModule`) — importing emit never drags a controller into the non-routed worker graph. **`PublicTimelineModule`** (in `PUBLIC_MODULES`) declares `TimelineController` on `@Controller('artworks')` (shares the base path with `ArtworksController`, disjoint method paths) → `GET /artworks/:id/timeline` + `?expand=true` + `?cursor=&limit=`. **Visibility tiers:** `visibility_tier` is a DB-**generated STORED** column from `event_type`, **fail-closed** (`ELSE 'expanded'`, derived from the single-source `DEFAULT_TIER_EVENT_TYPES`), so an expanded/admin event can never leak into the anonymous default view; `?expand=true` reveals expanded-tier **published** events (`is_published` DEFAULT **false** = opt-in publish). `additionalEventsCount` = whole-artwork published-expanded count, **first-page-only** (0 on paginated pages). **Keyset cursor** = opaque base64url `{v,o,i}`, row-value `(occurred_at,id) < (…)` over `occurred_at timestamptz(3)` (lossless ms round-trip), epoch-ms **range-bounded** to JS Date to avoid a RangeError→500 (`docs/solutions/code-review-patterns/keyset-cursor-epoch-bound-rangeerror.md`); malformed → `400 TIMELINE_INVALID_CURSOR`. Reuses TOV-189 soft-404/status-gate (`PUBLIC_VISIBLE_STATUSES`, non-UUID→404, no existence oracle, `Cache-Control: no-store`, own 30/min throttle). **PII:** read-side per-`event_type` key allowlist (`keyof`-tied) + discriminated write payloads; `secondary_trade` omits `txHash` (counterparty deanonymization). **Emit:** best-effort, post-commit, log-not-throw, idempotent via FULL-unique `source_ref` + `ON CONFLICT DO NOTHING`; fractionalization-deploy + secondary-trade-settlement writers ship, the other 7 event types are schema-only. Append-only guard trigger blocks hard/soft delete; forward-only (no backfill). Migration `1716000000047`. FE contract `docs/api-contracts/2026-08-24-tov191-artwork-timeline-api-contract.md`; deploy runbook `docs/solutions/deployment-issues/2026-08-24-tov191-artwork-timeline-deploy-runbook.md`; PR #51 review todos `todos/399-406-*`.
- **artists/** -- anonymous (`@Public()`) public browse [`api/v1/artists`]. Mock-data-first: real controller/service/DTO backed by an in-memory read repository behind a token (`IArtistReadRepository`), swappable for a TypeORM repo later (TOV-194). See `docs/solutions/code-review-patterns/nestjs-mock-data-first-public-read-module.md`.
- **files/** -- neutral files domain (entity/repo/`FilesService`) + public `FilesProxyController` (`api/v1/files`); admin CRUD is `backoffice/files` (`api/backoffice/v1/files`)
- **storage/** -- Supabase storage abstraction (`IStorageService`)
- **jobs/** -- BullMQ background job processing
- **backoffice/** -- admin surface: auth, admins, artworks, dashboard, files, kyc-allowlist, missions, offerings, stages, submissions, users [`api/backoffice/v1`]
