[package]
name = "tove-emergency-freeze"
description = "Emergency freeze set singleton (admin-gated) for Tove FractionToken gating (TOV-142)"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
