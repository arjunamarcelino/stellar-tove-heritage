[package]
name = "tove-registry-anchor"
description = "Daily Merkle-root anchoring singleton (admin-gated) for the Tove provenance registry (TOV-89)"
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
crate-type = ["cdylib", "rlib"]
doctest = false

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
